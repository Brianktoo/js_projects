#!/usr/bin/env node

let result = (20 > 0) ? true:false;
console.log(result)

let message = (20>19) ? "yes": "no";
console.log(message)

let marks=39
let pMark=(marks > 40) ? "pass": "fail";
console.log(pMark)
